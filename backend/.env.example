GEMINI_API_KEY=your_key_here
GROQ_API_KEY=your_key_here
# Optional: if set, every API request needs this password (see README).
# Leave unset for no login gate, e.g. local dev.
APP_PASSWORD=your_own_password_here